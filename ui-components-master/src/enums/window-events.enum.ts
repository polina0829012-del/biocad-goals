/**
 * События Window.
 * @enum {string}
 * @member {string} WindowEvents.scroll - Скролл.
 * @member {string} WindowEvents.resize - Изменение размера.
 */
export enum WindowEvents {
    scroll = 'scroll',
    resize = 'resize',
}
